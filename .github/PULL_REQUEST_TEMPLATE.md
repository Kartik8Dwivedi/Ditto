<!-- Thanks for contributing to Ditto! -->

## What & why

<!-- What does this change, and what problem does it solve? -->

Closes #

## Checklist

- [ ] `cd backend && npm run typecheck` passes
- [ ] `cd backend && npm test` passes (and I added/updated tests for the change)
- [ ] `cd frontend && npm run build` passes (if the frontend was touched)
- [ ] Tests are hermetic — no network, no live DB, no real API key (fixtures live in `backend/tests/fixtures/`)
- [ ] I kept the honesty rule intact — nothing labels a "suspected" result as "proven"

## Notes for the reviewer

<!-- Anything worth calling out: tradeoffs, follow-ups, screenshots for UI changes. -->
